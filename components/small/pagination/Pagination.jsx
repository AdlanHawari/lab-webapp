import { useEffect, useState } from "react"

export default function Pagination({paginator}) {
    // let arrayTotalPage = new Array(paginator.total_pages)
    // const [pageState, setPagestate] = useState([]);

    // function createArray(total_pages){
    //     for(var i=0; i<total_pages; i++){
    //         arrayTotalPage[i] = i+1
    //     }
    // }

    // useEffect(() => {
    //     createArray(paginator.total_pages)
    //     setPagestate(arrayTotalPage)
    //     console.log("pageState:", pageState)
    // },[])
    

  return (
    <div className="">
    </div>
  )
}
